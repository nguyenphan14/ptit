#include<bits/stdc++.h>
#define MAX 100
using namespace std;

int main() {
    int n, k, arr[MAX];
    cin >> n >> k;
    for(int i = 0; i < n; i++) {
        cin >> arr[i];
    }
    
    return 0;
}